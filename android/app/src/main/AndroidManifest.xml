<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Health Connect -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />
    <!-- Newer Health Connect blocks reads from background workers unless
         this separate permission is also granted (requested at runtime
         together with the Steps read permission). Without it the 15-min /
         4-min background sync gets SecurityException'd on Android 14+. -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_IN_BACKGROUND" />

    <!-- Step counter (pedometer + StepsFgService) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Foreground service (StepsFgService, health type) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Let the user whitelist the app from battery optimization so the
         step foreground service isn't killed in the background. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Reset the step notification to 0 at local midnight. Exact where
         granted (auto on API ≤32; user-grantable on 33+); the code falls
         back to an inexact while-idle alarm otherwise. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Notifications (FCM + local notifications) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Audio recording (speech page) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Google Advertising ID — required by the Meta SDK to attribute app
         events / installs to ads on Android 13+ (API 33+). -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" tools:node="remove" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:node="remove" />

    <application
        android:name="${applicationName}"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Calora">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="standard"
            android:theme="@style/LaunchTheme"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.google.android.apps.healthdata.permissions.api.sdk.HealthConnectPermissionActivity"
            android:exported="true" />

        <activity-alias
            android:name=".HealthPermissionActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            tools:node="merge">
            <intent-filter>
                <action android:name="androidx.health.platform.action.REQUEST_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

        <!-- Step-counting foreground service (the only FGS in the app —
             flutter_foreground_task was removed as unused). -->
        <service
            android:name=".StepsFgService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Midnight step-notification reset (scheduled via AlarmManager). -->
        <receiver
            android:name=".MidnightResetReceiver"
            android:exported="false" />

        <!-- Bring StepsFgService back up after reboot / app-update so
             daily counting resumes without needing the user to open the app. -->
        <receiver
            android:name=".StepsBootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Watchdog kick from the periodic WorkManager sync — restarts
             StepsFgService if an aggressive OEM killed it between the
             midnight-alarm resurrection points. -->
        <receiver
            android:name=".StepsWatchdogReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="ai.calora.app.steps.WATCHDOG_KICK" />
            </intent-filter>
        </receiver>

        <!-- Facebook / Meta SDK (App Events for ad targeting & attribution) -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <queries>
        <!-- Health Connect -->
        <package android:name="com.google.android.apps.healthdata" />
        <package android:name="com.google.android.apps.healthdata.debug" />

        <!-- ✅ Fitness app'lar shu yerga ko'chdi -->
        <package android:name="com.sec.android.app.shealth" />
        <package android:name="com.mi.health" />
        <package android:name="com.xiaomi.hm.health" />

        <!-- Health Connect intent'lari -->
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>

        <intent>
            <action android:name="androidx.health.platform.action.REQUEST_PERMISSIONS" />
        </intent>

        <intent>
            <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
            <data android:scheme="package" />
        </intent>

        <!-- Share intent'lari -->
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

</manifest>