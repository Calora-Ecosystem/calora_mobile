import 'package:flutter/material.dart';

class AccountDetailItemBuilder extends StatelessWidget {
  const AccountDetailItemBuilder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
